# The documentation build. Cloudflare Pages installs this on detecting it, and
# build.sh installs it into .venv-docs so that a local build matches.
#
# Fully pinned, transitive dependencies included, and generated with
# `pip freeze` from a clean virtualenv containing only `zensical`. Zensical's own
# metadata floats most of these (`markdown>=3.7`, `pymdown-extensions>=11.0`,
# `pygments>=2.20` and so on), so without pinning every deployment resolves a
# slightly different renderer and the site's output changes without a commit;
# highlighting spans in particular differ between Pygments releases.
#
# To bump: create a fresh virtualenv, install the zensical version you want, and
# replace this list with `pip freeze`. Do not hand-edit a single line, since the
# point is that the set is known to resolve together.
Jinja2==3.1.6
Markdown==3.10.3
MarkupSafe==3.0.3
Pygments==2.21.0
PyYAML==6.0.3
click==8.5.0
deepmerge==3.0.1
pymdown-extensions==11.0.2
tomli==2.4.1
zensical==0.0.59
